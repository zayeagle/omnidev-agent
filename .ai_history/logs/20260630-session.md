# 2026-06-30 Session Log

## Turn 1

**User**: `/od re`

**Actions**: Loaded session-memory.md; read session-log, 02-plan, 00-project-context; user chose restart then asked about agent premature completion.

**Key decision**: Analysis-only (no code changes).

**Assistant summary**: Delivered root-cause analysis of agent self-terminating before fulfilling user requirements.

## Turn 2

**User**: 更稳的进阶版 按照这个实现

**Actions**: Implemented acceptance gate system across agent/dispatcher/loop/TUI/checkpoint.

**Key files**: acceptance.go, outcome.go, loop.go, dispatcher.go, messages.go, checkpoint.go, turn.go, update.go

**Assistant summary**: Advanced completion verification shipped; all tests pass.

## Turn 2

**User**: README 过于详细，skill/MCP/tools/permissions 等内容不必要；保留 agent 简介和 quick start，详细参数配置移到说明文档。

**Actions**: 精简 `README.md`；新建 `docs/configuration-guide.md` 收纳原详细章节。

**Key decision**: README 只保留简介、特性、Quick Start、基础用法、最小配置示例与文档链接；完整配置/工具/权限/Skills/MCP 等迁至独立指南。

**Assistant summary**: README 从 ~780 行精简至 ~120 行；详细内容见 `docs/configuration-guide.md`。

## Turn 3

**User**: Cursor 如何只读文件部分行；需要 omnidev-agent 实现「先搜再读」策略。

**Actions**: 新增 `internal/agent/prompts.go`（`codeExplorationGuidance`）；`buildMessages` 注入 system prompt；强化 `read_file` / `search_code` 工具描述与参数说明；`prompts_test.go` 断言。

**Key decision**: search_code 返回 file:line → read_file 用 offset/limit 读窗口；大文件禁止盲目全文读。

**Assistant summary**: 与 Cursor 一致的两步探索策略已写入 prompt 与工具 schema；测试通过。

## Turn 4

**User**: 继续（按 8.5+/10 ROI 路线图六项优化）

**Actions**: 完成验收可靠性进阶六项：子任务 Contract 对齐（≥2 reads + write）；自定义 verify 命令（`verify:` / 验收 / backtick）；LLM judge 失败不再 heuristic 放行；严格 agentLoop E2E；成功结论仅 criteria 证据；exit gate nudge + checkpoint 恢复（`ExitGateNudges` / `AcceptanceIncomplete` / `Resumable`）。

**Key files**: acceptance.go, verify_commands.go, loop.go, conclusion.go, dispatcher.go, checkpoint.go, messages.go, acceptance_e2e_test.go, verify_commands_test.go

**Assistant summary**: `go test ./...` 全部通过；可靠性目标 8.5+/10 路线图落地。

## Turn 5

**User**: 继续优化 P0/P1 四项 + 询问模型连接失败/网络中断是否会一直重连

**Actions**: LLM judge 机械交叉校验；search/list 计入探索；TUI Resumable 提示；子 Agent 统一 verify-fix；Stream 路径增加有界重试并文档化。

**Key files**: acceptance.go, dispatcher.go, messages.go, update.go, chat.go

**Assistant summary**: 四项优化落地；LLM 默认最多 4 次尝试（非无限重连），`llm_max_retries` 可配置。

## Turn 6

**User**: 继续 — 网络错误默认持续重连

**Actions**: `retry_loop.go` 网络错误无限重试（直至成功或 Ctrl+C）；`isNetworkError` 分类；TUI 显示重连倒计时；配置 `llm_persist_network_retry` / `OMNIDEV_LLM_PERSIST_NETWORK_RETRY`。

**Assistant summary**: 网络错误默认持续 auto-reconnect；非网络/4xx 仍受 `llm_max_retries` 限制。`go test ./...` 通过。

## Turn 7

**User**: 验收不通过不应中断交给用户，Agent 应自主分析原因并继续迭代，最终结果必须完整

**Actions**: 移除 exit gate 2 次 nudge 硬停；验收失败一律 `continue` 回 ReAct 循环；新增 `driveUntilAccepted` 自主恢复（最多 50 轮，每轮额外 turns）；`finishParentTask` / dispatcher 改用恢复循环而非 `acceptance criteria not satisfied` 中断。

**Key files**: acceptance.go, acceptance_recovery.go, loop.go, dispatcher.go

**Assistant summary**: 验收失败 → 注入缺口分析 → 自动继续干活，仅 LLM 致命错误/取消/权限拒绝才会停。

## Turn 8

**User**: 要支持可以翻用户提出历史问题

**Actions**: 普通 ↑↓ 浏览历史用户提问（不含 `/help` 等 slash 命令）；Ctrl/Alt+↑↓ 与 PgUp/PgDn 滚动 transcript；会话恢复时从 turns 重建 history；`/clear` 清空 history。

**Key files**: `internal/tui/components/input.go`, `update.go`, `persist.go`, `tui.go`, `header.go`, `permission.go`

**Assistant summary**: 输入框支持 shell 式历史翻页；`go test ./internal/tui/...` 通过。

## Turn 9

**User**: 发现一个很严重的问题，为什么会出现持续读文件，而且相同的文件读了很多次

**Root cause**: 上下文裁剪把旧 read_file 变成「请再 read_file」→ LLM 反复读；验收恢复循环 + 分析类任务误触 build/write 验收 → 无限迭代读文件。

**Fixes**: read_file 会话缓存去重；裁剪保留内容预览而非「请重读」；system 提示已读文件列表；review 专用验收标准；read-only 跳过 writes 交叉校验；默认保留 8 条完整 tool 结果。

**Assistant summary**: `go test ./...` 通过。需重新编译运行。

## Turn 10

**User**: Thinking 状态太久，不知道在干什么，没有过程信息；是否默认展开 Thinking

**Fixes**: ActivityMsg 实时活动（Calling model turn N/M、Running tools、验收/恢复）；Working 指示器显示详情；重要事件写入 status 区；流式时自动展开 Thinking；折叠时预览 2 行。

**Assistant summary**: `go build ./...` 通过。

## Turn 11

**User**: 过程状态应是 Working 非 Thinking；Executing 改 Working；验收期间打印标准并逐项勾选

**Changes**: StateExecuting/StateVerifying 显示 Working；WorkingLabel/activity 区分 LLM Thinking vs 工具/验收 Working；验收开始 InitChecklist (○)，逐项 emitCriterionChecked (✓/✗)；TUI Acceptance criteria 清单。

**Assistant summary**: `go test ./internal/tui/...` 通过。

## Turn 12

**User**: 是不是进入死循环；贪吃蛇能正常运行但验收不过；验收标准是否与实际不符；要求全过程详细日志（部分控制台 + 全部写入二进制目录日志文件）

**Root cause**: 非死循环，是验收恢复循环。自动追加的 verify 子任务要求 `go build ./...` 与 `go test ./...` 全部通过；游戏可 `go run` 运行，但 `go test ./...` 有 3 个失败 → 每轮验收失败 → 注入 recovery → agent 继续改代码/读文件。顶部 Todo 勾选与 acceptance criteria 不是同一套状态。

**Fixes**:
- build/test 拆分：`BuildOK` / `TestOK` / `TestsRan`，测试失败时明确提示「游戏可手动运行但单测未过」
- 每轮验收失败打印 `formatAcceptanceReport`（每项 PASS/FAIL + reason + mechanical summary）
- 相同失败连续 3 次 → 停止自动恢复并提示 `[验收停滞]`
- 最多 15 轮 recovery
- `internal/runlog`：二进制同目录 `omnidev-agent-YYYYMMDD-HHMMSS.log`，全量记录；TUI footer 显示路径
- 修复编译：重复 `SetAcceptanceStrict`、`acceptance_recovery.go` 缺 `context` import

**Assistant summary**: `go test ./...` 全部通过。需重新编译运行 agent 验证。

## Turn 13

**User**: 最终生成的项目文件夹名称没有按照功能需求名称来定义，之前是可以的

**Root cause**: `DeriveProjectDir` 对纯中文且未命中关键词（如「写一个终端小游戏」）会 fallback 为 `project-20260630-224149`；仅提取 ASCII 词；后续轮次可能用新 timestamp 覆盖 outputDir。

**Fixes**: 增加 `chinesePhraseSlugs` / `chineseTokenSlugs`；`DeriveProjectDirFromTexts` 遍历会话所有 user 消息；follow-up 复用已有 workspace；`setupProjectWorkspace` 统一创建逻辑；`生成`/`写一个` 加入 newProjectVerbs。

**Assistant summary**: `go test ./...` 通过。重新编译后「贪吃蛇」→ `snake-game`，「终端小游戏」→ `terminal-game`。

## Turn 14

**User**: 单元测试验收不过，是程序问题还是路径这些都要去分析，不能一直怀疑程序问题

**Fixes**: 新增 `verify_diagnosis.go` 失败根因分类（path / environment / test / program）；验收报告输出 `Failure diagnosis`（证据 + 有序建议）；恢复/verify-fix 提示要求先分类再改代码；停滞时提示勿把路径/环境问题当程序 bug 反复改逻辑。

**Assistant summary**: `go test ./...` 通过。

## Turn 15

**User**: 为什么会出现不断地读这个文件（game.go 反复 read_file）

**Root cause**: 验收恢复循环（turn 13/20 + verify 子任务）导致 edit→read→edit；读缓存按 path+offset+limit 键，参数变化会绕过；edit 后缓存未失效。

**Assistant summary**: `go test ./...` 通过。

## Turn 16

**User**: Calling model (turn 8/20) 是什么意思，为什么一直这个状态，应该是 Working

**Explanation**: 正在等 LLM 第 8/20 轮响应（分析 test 失败后决定下一步），不是卡死；93% 上下文 + 带 tools 请求较慢。

**Fixes**: 工具循环内 LLM 等待改为 `StateExecuting` + `Working · calling model (turn N/M)…`；纯对话仍 Thinking。

**Assistant summary**: `go test ./...` 通过。

## Turn 17

**User**: 验收报告默认折叠；上下两部分重复；验收过程应逐项勾选并支持跳过

**Explanation**: 上部 `── Acceptance verification ──` 来自 formatAcceptanceReport；下部 `[x] …` 来自 formatConclusionFromCriteria，内容重复。

**Fixes**: 完成面板默认折叠 `▸ 验收通过 N/N · 查看详细`（点击展开）；验收中顶部 pinned checklist 逐项 ○→✓/−；200ms 间隔更新；无 _test.go 时标记 skipped。

**Assistant summary**: `go test ./...` 通过。

## Turn 18

**User**: 全部优化；runlog 是否足够详细，不够则继续增强；优化耗时耗 token 环节

**Checklist implemented (P0–P2)**:
- P0: `go test` 通过时用测试证据满足功能验收，跳过 LLM judge 误判；未明确要求单元测试时 verify 任务仅 `go build`
- P1: 阻止大块 `write_file` 重写已有 `_test.go`；`edit_file` 失败提示一次全文件 read；机械验证通过+测试覆盖时 recovery 短路
- P2: 默认关闭 `UseLLMAcceptance`；runlog 仅记录工具元数据（路径/字节/行数），新增 `acceptance_decision` / `read_cache` / `recovery` / turn 耗时等

**Runlog 新增字段示例**:
- `[llm] turn N/M request msgs=…` / `response duration=… tool_calls=…`
- `[acceptance_decision] source=heuristic+test_evidence|llm+test_evidence|test_evidence_skip_llm`
- `[read_cache] CACHED|THROTTLED|INVALIDATE path=…`
- `[verify] mechanical … tools writes/reads … diagnosis kind=…`
- `[recovery] skipped reason=mechanical_green+test_evidence`

**Assistant summary**: `go test ./...` 全部通过。需重新编译安装二进制后新 runlog 与优化才生效。
