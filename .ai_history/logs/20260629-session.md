## 2026-06-29 — To-dos expand + single active session resume

### User
- 点击 To-dos 无法展开
- 未归档会话关闭后重启应恢复历史上下文；仅 `/archive` 后新建对话
- **补充**：只恢复最新一条未归档会话

### Assistant 实现
1. **To-dos 展开**：默认启用鼠标；修正行号计算；空输入时 Enter/t 也可展开
2. **会话持久化**：`.ai_history/sessions/_active.json` 保存唯一进行中会话；启动时 `LoadActive`；退出/任务结束 `SaveActive`；`/archive` 写入 `{id}.json`+`.md` 并清除 active

### 测试
`go test ./...` 通过
